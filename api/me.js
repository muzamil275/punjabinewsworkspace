const { json, cors, requireUser, isOwner, supabaseFetch, dbJson } = require('../lib/api');
module.exports = async (req, res) => { cors(req, res); if (req.method === 'OPTIONS') return res.status(204).end(); if (req.method !== 'GET') return json(res, { error: 'Method not allowed.' }, 405); try { const user = await requireUser(req, res); if (!user) return; const owner = isOwner(user); const r = await supabaseFetch(`subscriptions?user_id=eq.${encodeURIComponent(user.id)}&select=plan,status,access_ends_at&limit=1`, { authHeader: req.headers.authorization || '' }); const rows = r.ok ? await dbJson(r) : []; const sub = rows[0] || null; const active = owner || Boolean(sub && new Date(sub.access_ends_at).getTime() > Date.now() && ['provisional','active'].includes(sub.status)); return json(res, { user: { id: user.id, email: user.email || null, phone: user.phone || null, isAdmin: owner, isOwner: owner }, subscription: owner ? { plan: 'premium', status: 'active', access_ends_at: null, active: true, owner: true } : (sub ? { ...sub, active } : null) }); } catch (e) { return json(res, { error: e.message || 'Account request failed.' }, 500); } };
