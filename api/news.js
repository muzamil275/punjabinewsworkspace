const { json, cors, supabaseFetch, dbJson } = require('../lib/api');
module.exports = async (req, res) => { cors(req, res); if (req.method === 'OPTIONS') return res.status(204).end(); if (req.method !== 'GET') return json(res, { error: 'Method not allowed.' }, 405); try { const r = await supabaseFetch('news_posts?is_published=eq.true&select=id,category,title_en,title_ur,excerpt_en,excerpt_ur,published_on,daily_rank&order=published_on.desc,daily_rank.asc&limit=5'); const data = await dbJson(r); if (!r.ok) return json(res, { error: 'News is temporarily unavailable.' }, 503); return json(res, { posts: data, language: req.query?.lang === 'ur' ? 'ur' : 'en' }); } catch (e) { return json(res, { error: e.message || 'News request failed.' }, 500); } };
