const { json, cors, requireUser, isOwner, supabaseFetch, dbJson, karachiDate, safeText, safeUrl } = require('../../lib/api');
function fields(body) { const imageRaw=String(body.imageUrl||'').trim(), sourceRaw=String(body.sourceUrl||'').trim(); return { category:safeText(body.category,40),titleEn:safeText(body.titleEn,160),titleUr:safeText(body.titleUr,160),excerptEn:safeText(body.excerptEn,500),excerptUr:safeText(body.excerptUr,500),imageUrl:imageRaw?safeUrl(imageRaw):null,sourceName:sourceRaw?safeText(body.sourceName,80):safeText(body.sourceName,80),sourceUrl:sourceRaw?safeUrl(sourceRaw):null,rank:Number(body.dailyRank),invalidImage:Boolean(imageRaw&&!safeUrl(imageRaw)),invalidSource:Boolean(sourceRaw&&!safeUrl(sourceRaw)) }; }
function valid(f){return f.category&&f.titleEn&&f.titleUr&&f.excerptEn&&f.excerptUr&&Number.isInteger(f.rank)&&f.rank>=1&&f.rank<=5&&!f.invalidImage&&!f.invalidSource;}
module.exports=async(req,res)=>{cors(req,res);if(req.method==='OPTIONS')return res.status(204).end();try{const user=await requireUser(req,res);if(!user)return;if(!isOwner(user))return json(res,{error:'Owner access required.'},403);const authHeader=req.headers.authorization||'';if(req.method==='GET'){const r=await supabaseFetch(`news_posts?published_on=eq.${encodeURIComponent(karachiDate())}&select=id,category,title_en,title_ur,excerpt_en,excerpt_ur,image_url,source_name,source_url,published_on,daily_rank,updated_at&order=daily_rank.asc`,{authHeader});const data=await dbJson(r);return r.ok?json(res,{posts:Array.isArray(data)?data:[]}):json(res,{error:'Owner news list unavailable.'},503);}const id=String(req.query?.id||'');if(req.method==='POST'){const f=fields(req.body||{});if(!valid(f))return json(res,{error:'Complete the bilingual fields, use a rank from 1 to 5, and provide valid HTTPS URLs.'},422);const r=await supabaseFetch('news_posts',{method:'POST',authHeader,headers:{Prefer:'return=representation'},body:JSON.stringify({category:f.category,title_en:f.titleEn,title_ur:f.titleUr,excerpt_en:f.excerptEn,excerpt_ur:f.excerptUr,image_url:f.imageUrl,source_name:f.sourceName,source_url:f.sourceUrl,daily_rank:f.rank,published_on:karachiDate(),is_published:true,published_by:user.id})});if(!r.ok)return json(res,{error:'That rank is already used today, or publishing failed.'},409);return json(res,{ok:true},201);}if(!/^\d+$/.test(id))return json(res,{error:'A valid news ID is required.'},422);if(req.method==='PATCH'){const f=fields(req.body||{});if(!valid(f))return json(res,{error:'Complete the bilingual fields, use a rank from 1 to 5, and provide valid HTTPS URLs.'},422);const r=await supabaseFetch(`news_posts?id=eq.${encodeURIComponent(id)}&published_on=eq.${encodeURIComponent(karachiDate())}`,{method:'PATCH',authHeader,headers:{Prefer:'return=minimal'},body:JSON.stringify({category:f.category,title_en:f.titleEn,title_ur:f.titleUr,excerpt_en:f.excerptEn,excerpt_ur:f.excerptUr,image_url:f.imageUrl,source_name:f.sourceName,source_url:f.sourceUrl,daily_rank:f.rank,updated_at:new Date().toISOString()})});if(!r.ok)return json(res,{error:'News update failed. The rank may already be in use.'},409);return json(res,{ok:true});}if(req.method==='DELETE'){const r=await supabaseFetch(`news_posts?id=eq.${encodeURIComponent(id)}&published_on=eq.${encodeURIComponent(karachiDate())}`,{method:'DELETE',authHeader,headers:{Prefer:'return=minimal'}});if(!r.ok)return json(res,{error:'News deletion failed.'},503);return json(res,{ok:true});}return json(res,{error:'Method not allowed.'},405);}catch(e){return json(res,{error:e.message||'News administration failed.'},500);}};
